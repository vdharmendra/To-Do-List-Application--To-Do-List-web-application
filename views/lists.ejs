<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= listTitle %> - Tasks</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/css/style.css" rel="stylesheet" />
</head>
<body>
    <div class="bg-light min-vh-100 d-flex flex-column">
        <div class="container py-5">
            <h1 class="text-center mb-4 display-4 fw-bold"><%= listTitle %> Tasks List</h1>

            <div class="tasks-list mb-4">
                <% if (newListItems.length === 0) { %>
                    <p class="text-muted text-center fst-italic">No tasks yet. Add something below!</p>
                <% } else { %>
                    <% for(let i = 0; i < newListItems.length; i++) { %>
                        <div class="task-item card shadow-sm mb-2">
                            <div class="card-body d-flex align-items-center">
                                <input 
                                    type="checkbox" 
                                    class="form-check-input me-3 task-checkbox" 
                                    id="task<%= i %>" 
                                    data-index="<%= i %>"
                                    aria-label="Mark <%= newListItems[i] %> as completed"
                                />
                                <label class="form-check-label mb-0 flex-grow-1 task-label" for="task<%= i %>">
                                    <%= newListItems[i] %>
                                </label>
                            </div>
                        </div>
                    <% } %>
                <% } %>
            </div>

            <form action="/" method="post" class="d-flex gap-2" aria-label="Add new task form">
                <input 
                    type="text" 
                    class="form-control form-control-lg" 
                    name="newItem" 
                    placeholder="Add New Task..." 
                    autocomplete="off" 
                    required 
                    aria-required="true"
                />
                <button 
                    class="btn btn-primary btn-lg px-4" 
                    type="submit" 
                    name="submit" 
                    value="<%= listTitle %>"
                >
                    Add
                </button>
            </form>
        </div>
        <footer class="text-center py-3 text-muted">
            &copy; <%= new Date().getFullYear() %> Food Travel Tales To-Do App
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Fix checkbox toggle effect on task label
        document.querySelectorAll('.task-checkbox').forEach((checkbox) => {
            checkbox.addEventListener('change', function() {
                const label = this.nextElementSibling;
                if (this.checked) {
                    label.classList.add('completed');
                } else {
                    label.classList.remove('completed');
                }
            });
        });
    </script>
</body>
</html>